name: Deploy to Azure Function

# main'e her push'ta (kod dosyaları değişince) Function App'e otomatik deploy.
# Kurulum (bir kere):
#   - Repo variable: AZURE_FUNCTIONAPP_NAME = <function app adı>
#   - Repo secret:   AZURE_FUNCTIONAPP_PUBLISH_PROFILE = <publish profile XML>
# Değişken tanımlı değilse (fork/müşteri) job atlanır.
on:
  push:
    branches: [main]
    paths:
      - "**.py"
      - "host.json"
      - "requirements.txt"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ vars.AZURE_FUNCTIONAPP_NAME != '' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Deploy to Azure Functions
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ vars.AZURE_FUNCTIONAPP_NAME }}
          package: .
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
          scm-do-build-during-deployment: true
          enable-oryx-build: true
